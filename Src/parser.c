#include "../Inc/minishell.h"


/**
 *
 * @brief: Esta funcion pretende recoger el listado de tokens con su calificacion sintactica y rellenar un arbol de ejecucion
 *
 *
 *
 *
 */
t_tree *parser(t_token* tokens) {
    t_tree* data_tree = 0x0;
    (void)tokens;

    return data_tree; 
}
