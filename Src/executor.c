#include "../Inc/minishell.h"

void executor(t_tree* data_tree, char **env) {
    (void)data_tree;
    (void)env;
    // if (data_tree->right)
    //     executor(data_tree->right, env);
    // else
    //     data_tree->cmd = env[0];
}
